//This js file is used to generate the virtual space that is defined by
//the data obtained from the csv files


function buildWorld()
{
  console.log("Within build: " + data[1][1]);
  //Do cool stuff using the data points here.
  /*
  As a note data is stored as a 2d array.
  The first element is the data set for a example a book,
  and the second data set is the info, so for example the number of pages or
  whatever is stored in the csv file.
  */
}
